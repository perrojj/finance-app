git init
git clone https://github.com/perrojj/finance-app/
entrar na pasta de frontend e colocar npm install no console e depois npm run dev

no back end python -m venv venv
(No Windows (Prompt de Comando comum): venv\Scripts\activate

No Windows (PowerShell): .\venv\Scripts\activate

No Linux ou Mac: source venv/bin/activate)

pip install fastapi uvicorn sqlalchemy pydantic

uvicorn main:app --reload

python -m uvicorn main:app --reload


.\venv\Scripts\activate
